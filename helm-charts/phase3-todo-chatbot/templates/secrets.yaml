{{- if .Values.secrets.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.secrets.name }}
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "phase3.labels" . | nindent 4 }}
type: Opaque
stringData:
  # Database Credentials
  POSTGRES_USER: {{ .Values.secrets.postgresUser | quote }}
  POSTGRES_PASSWORD: {{ .Values.secrets.postgresPassword | default (randAlphaNum 32) | quote }}
  POSTGRES_DB: {{ .Values.secrets.postgresDB | quote }}
  
  # Database Connection String
  DATABASE_URL: {{ include "phase3.databaseUrl" . | quote }}
  
  # Authentication
  BETTER_AUTH_SECRET: {{ .Values.secrets.betterAuthSecret | default (randAlphaNum 32) | quote }}
  
  # OpenAI API Key (optional)
  OPENAI_API_KEY: {{ .Values.secrets.openaiApiKey | quote }}
{{- end }}
