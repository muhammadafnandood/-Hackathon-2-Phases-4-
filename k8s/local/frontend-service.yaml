apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: phase3
  labels:
    app: frontend
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: frontend
