apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: phase3
  labels:
    app: backend
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 4000
      protocol: TCP
      name: http
  selector:
    app: backend
